[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "gradientnorm-autoclipper"
version = "1.0.0"
description = "Clip gradient norm automatically"
readme = "README.md"
authors = [{name = "Michał Dyczko", email = "michal@dyczko.dev"}]
license = {file = "LICENSE"}
keywords = ["pytorch", "gradient clipping", "automl"]
dependencies = [
    "torch>=2.2.1",
    "pytorch_optimizer>=3.0.0"
]
requires-python = ">=3.8"

[project.urls]
Homepage = "https://github.com/michaldyczko/gradientnorm-autoclipper"