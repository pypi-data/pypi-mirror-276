def oppg_strek(on = -1, typ = 1):
    if on == 1 and typ == 1: print(f""); print(f"------------------------------------------") # pass
    if on == 1 and typ == 2: print(f""); print(f"******************************************") # pass

# Alias > 1 > ...

# Alias > 2 > ...
strek_oppg = oppg_strek
