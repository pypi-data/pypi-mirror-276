[tool.poetry]
name = "PSO_Logger"
version = "1.0.1"
description = "This is a Logging Library for PSO Projects."
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.8"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
