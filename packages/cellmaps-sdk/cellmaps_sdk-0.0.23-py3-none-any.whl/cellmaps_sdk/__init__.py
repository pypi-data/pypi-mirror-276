import os

# For local development, this flag if True, makes the cellmaps sdk write to local files instead of the object storage, etc

# Need to figure out how to populate these things automatically
__version__ = '0.1.0' #type: ignore
__library_name__ = 'cellmaps_sdk' #type: ignore