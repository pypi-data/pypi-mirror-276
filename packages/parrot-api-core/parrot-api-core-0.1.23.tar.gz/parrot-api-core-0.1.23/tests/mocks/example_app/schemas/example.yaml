openapi: 3.0.0
info:
  title: Example Apis
  version: '1.0'
servers:
  - url: /v1/
paths:
  /hello:
    get:
      operationId: parrot_api.core.server.hello
      responses:
        200:
          description: returns what you send
          content:
            application/json:
              schema:
                type: object
