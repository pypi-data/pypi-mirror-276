[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "Flask-Apium"
version = "0.0.1"
readme = "README.md"
dependencies = [
  'flask',
  'celery[sqlalchemy]',
]
