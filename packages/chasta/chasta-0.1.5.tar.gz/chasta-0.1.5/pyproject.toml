[tool.poetry]
name = "chasta"
version = "0.1.5"
description = "CHArt and run STAtitics on CSV data"
authors = ["Jerome Provensal <jeromegit@provensal.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
pandas = "^2.0.3"
plotly = "^5.15.0"

[tool.poetry.scripts]
chasta = 'chasta.chasta:main'

[tool.poetry.dev-dependencies]
pytest = "^7.4.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
