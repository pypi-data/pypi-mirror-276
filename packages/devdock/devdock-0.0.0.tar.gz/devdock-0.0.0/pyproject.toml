[tool.poetry]
name = "devdock"
version = "0.0.0"
description = "A Python library for managing development containers."
authors = ["Ali Tavallaie <a.tavallaie@gmail.com>"]
license = "mit"
#readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
