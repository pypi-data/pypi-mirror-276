[tool.poetry]
name = "clipdetect"
version = "0.1.4"
description = ""
license = "MIT"
authors = ["roedoejet <hello@aidanpine.ca>"]
readme = "README.md"

[tool.poetry.scripts]
clipdetect = "clipdetect.cli:app"

[tool.poetry.dependencies]
python = ">=3.9"
typer = ">=0.9.0"
rich = ">=10.11.0"
shellingham = ">=1.3.0"
numpy = ">=1.22"
scipy = "^1.10.0"
tqdm = "^4.64.1"
pympi-ling = "^1.70.2"
loguru = "^0.6.0"
pytest = "^7.2.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
