from setuptools import setup, find_packages

setup(
    name='lcprogramtools',
    version='0.0.2',
    description='A Python package which does exactly what the name says',
    author='Subham Sarangi',
    author_email='ivan.sarangi@gmail.com',
    packages=find_packages(),
    python_requires='>=3.6',
    install_requires=[],
)
