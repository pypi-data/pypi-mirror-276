[tool.poetry]
name = "aixblock_crawler"
version = "0.0.2"
description = "aixblock_crawler"
authors = ["TonyShark <quoi@wow-ai.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"

[build-system]
requires = ["poetry-core"] 
build-backend = "poetry.core.masonry.api"

